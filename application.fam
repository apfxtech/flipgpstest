App(
    appid="gps_rpc",
    name="[RPC] GPS",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="gps_app",
    requires=["gui"],
    stack_size=2 * 1024,
    order=35,
    fap_icon="gps_10px.png",
    fap_category="GPIO",
    fap_author="@ezod & @xMasterX",
    fap_version="2.0",
    fap_description="Shows the companion phone's location over RPC (GPS API, 5 Hz stream).",
)
